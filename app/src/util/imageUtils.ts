export function imageUtils(sceneName: string) {
    switch(sceneName) { 
        case 'New City Park': { 
           return '/games/city-park.png';
           break; 
        } 
        default: { 
           break; 
        } 
     } 
}